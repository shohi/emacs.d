;; -*- coding: utf-8; lexical-binding: t; -*-
;; The mode displays the key bindings following your currently entered
;; incomplete command (aprefix) in a popup

(use-package which-key
  :ensure t
  :config
  (which-key-mode))

(provide 'init-whichkey)
;;; init-whichkey ends here.
