;; -*- coding: utf-8; lexical-binding: t; -*-
;; Beacon — Never lose your cursor again.

(use-package beacon
  :ensure t
  :config
  (beacon-mode 1))

(provide 'init-beacon)
;;; init-beacon ends here.
